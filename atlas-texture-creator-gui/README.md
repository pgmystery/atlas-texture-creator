# Atlas Texture Creator GUI

## Problems:
### Ubuntu:
Error: \
`qt.qpa.plugin: Could not load the Qt platform plugin "xcb" in "" even though it was found`

FIX:
```shell
sudo apt install libxcb-cursor0
```
